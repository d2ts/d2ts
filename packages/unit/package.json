{
  "name": "@d2ts/unit",
  "version": "0.0.1",
  "description": "TypeScript library for D2BS (@unit)",
  "license": "ISC",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && npm run compile",
    "compile": "rollup -c ../../rollup.config.js",
    "clean": "rm -rf ./dist"
  },
  "devDependencies": {
    "@d2ts/types": "^0.0.1",
    "typescript": "^3.9.2"
  }
}
